{
  "name": "zebkit-docs",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:tokens": "npm run build:tokens --prefix .. -- --config zebkit.docs.config.json",
    "predev": "npm run sync:tokens",
    "dev": "astro dev",
    "prestart": "npm run sync:tokens",
    "start": "astro dev",
    "prebuild": "npm run sync:tokens",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.36.3",
    "astro": "^5.5.0",
    "starlight-theme-rapide": "^0.5.2"
  }
}
